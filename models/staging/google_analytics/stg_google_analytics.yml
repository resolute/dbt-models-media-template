version: 2

models:
  - name: stg_google_analytics__session_performance
    description: A table containing daily session performance data from Google Analytics
    columns:
      - name: id
        description: Surrogate primary key
        tests:
          - unique
          - not_null