version: 2

models:
  - name: executive_summary
    description: A table containing the daily aggregated totals for each digital platform
    columns:
      - name: id
        description: This is a surrogate key
        tests:
          - unique
          - not_null