version: 2

models:
  - name: rollup_paid_media__campaigns
    description: A table containing the daily aggregated totals for each paid media platform
    columns:
      - name: id
        description: This is a surrogate key
        tests:
          - unique
          - not_null