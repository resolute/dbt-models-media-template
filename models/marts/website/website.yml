version: 2

models:
  - name: website_session_performance
    description: A table containing session performance data from Google Analytics properties
    columns:
      - name: id
        description: Surrogate primary key
        tests:
          - unique
          - not_null