version: 2

models:
  - name: social_paid__campaigns_rollup
    description: A table containing the daily totals for each social platform's campaigns
    columns:
      - name: id
        description: This is a surrogate key
        tests:
          - unique
          - not_null