version: 2

models:
  - name: pinterest_ads__pins_daily
    description: A table containing the daily totals for each Pinterest ads ad
    columns:
      - name: id
        description: This is a surrogate key
        tests:
          - unique
          - not_null