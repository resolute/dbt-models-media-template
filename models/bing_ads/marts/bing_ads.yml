version: 2

models:
  - name: bing_ads__ads_performance_daily
    description: A table containing the daily totals for each Bing Ads ad
    columns:
      - name: id
        description: Surrogate primary key
        tests:
          - unique
          - not_null

  - name: bing_ads__keywords_performance_daily
    description: A table containing the daily totals for each Bing Ads keyword
    columns:
      - name: id
        description: Surrogate primary key
        tests:
          - unique
          - not_null

  - name: bing_ads__campaigns_performance_daily
    description: A table containing the daily totals for each Bing Ads keyword
    columns:
      - name: id
        description: Surrogate primary key
        tests:
          - unique
          - not_null          