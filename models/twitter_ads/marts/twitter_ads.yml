version: 2

models:
  - name: twitter_ads__promoted_tweets_performance_daily
    description: A table containing the daily totals for each Twitter Ads promoted tweet.
    columns:
      - name: id
        description: Surrogate primary key
        tests:
          - unique
          - not_null