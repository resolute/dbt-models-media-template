version: 2

models:
  - name: rollup_search_paid__campaigns
    description: A table containing the daily totals for each paid search platform's campaigns
    columns:
      - name: id
        description: Surrogate primary key
        tests:
          - unique
          - not_null